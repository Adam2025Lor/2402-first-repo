name: Call Reusable Workflow
on:
  push:
    branches:
      - main
      - master

jobs:
  use-reusable:
    uses: Adam2025Lor/2402-first-repo/new/master/.github/workflows/reusable-test.yaml@master
    with:
      cool_var: "amazing"
